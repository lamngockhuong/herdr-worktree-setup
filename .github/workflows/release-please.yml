name: Release

on:
  push:
    branches: [main]

# The action opens a release pull request and, once it is merged, creates the
# tag and the GitHub release.
permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@v5
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
